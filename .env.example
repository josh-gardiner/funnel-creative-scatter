# SSW Platforms Data Hub API key (server-side only)
PDH_API_KEY=pdh_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: shared secret for the daily cache pre-warm cron (/api/prewarm).
# When set, the endpoint requires "Authorization: Bearer <CRON_SECRET>".
CRON_SECRET=

